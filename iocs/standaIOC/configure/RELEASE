# RELEASE - Location of external support modules

# Use motor/module's generated release file when buidling inside motor
-include $(TOP)/../../../RELEASE.$(EPICS_HOST_ARCH).local
# Use motorStanda's release file when building inside motorStanda, but outside motor
-include $(TOP)/../../configure/RELEASE.local
# Use standaIOC's RELEASE.local when building outside motorStanda
-include $(TOP)/configure/RELEASE.local
